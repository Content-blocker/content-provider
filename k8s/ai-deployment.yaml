apiVersion: apps/v1
kind: Deployment
metadata:
  name: blocker-ai-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blocker-ai
  template:
    metadata:
      labels:
        app: blocker-ai
    spec:
      containers:
        - image: docker.io/sr123/ai:ai-1.0.0-SNAPSHOT-16.1
          name: blocker-ai
          env:
            - name: KUMULUZEE_DISCOVERY_CLUSTER
              value: gcloud
            - name: KUMULUZEE_DISCOVERY_ETCD_HOSTS
              value: http://34.89.142.186:2379
            - name: KUMULUZEE_CONFIG_ETCD_HOSTS
              value: http://34.89.142.186:2379
            - name: KUMULUZEE_SERVER_BASEURL
              value: http://35.240.79.227:8082
            #- name: KUMULUZEE_DATASOURCES0_CONNECTIONURL
            #  value: jdbc:postgresql://34.89.221.63:5432/postgres
            #- name: KUMULUZEE_DATASOURCES0_USERNAME
            #  value: postgres
            #- name: KUMULUZEE_DATASOURCES0_PASSWORD
            #  valueFrom:
            #    secretKeyRef:
            #     name: pg-pass
            #     key: KUMULUZEE_DATASOURCES0_PASSWORD
          ports:
            - containerPort: 8082
              name: server
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /ai/health/live
              port: server
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ai/health/ready
              port: server
            initialDelaySeconds: 20
            periodSeconds: 10
---

apiVersion: v1
kind: Service
metadata:
  name: blocker-ai
spec:
  type: NodePort
  ports:
    - port: 8082
      protocol: TCP
      targetPort: server
  selector:
    app: blocker-ai